# perf-libs — sub-library definitions
#
# Each entry maps to a build script in packages/<id>/build.sh
# The build order is resolved automatically via topological sort on depends_on.
#
# Fields:
#   id          — directory name under packages/
#   depends_on  — list of ids that must be built before this one
#   arch        — restrict to x86 or arm (omit = both)
#   provides    — per-distro package names this lib satisfies
#                 (also used as conflicts + replaces)

libraries:
  - id: llvm
    provides:
      ubuntu-lts: [libllvm18]
      debian-stable: [libllvm19]
      fedora-latest: [llvm-libs]
      arch: [llvm-libs]

  - id: libdrm
    provides:
      ubuntu-lts: [libdrm2, libdrm-common, libdrm-amdgpu1, libdrm-intel1, libdrm-nouveau2, libdrm-radeon1]
      debian-stable: [libdrm2, libdrm-common, libdrm-amdgpu1, libdrm-intel1, libdrm-nouveau2, libdrm-radeon1]
      fedora-latest: [libdrm]
      arch: [libdrm]

  - id: mesa
    depends_on: [llvm, libdrm]
    provides:
      ubuntu-lts: [libgl1-mesa-dri, libglx-mesa0, libegl-mesa0, mesa-vulkan-drivers, mesa-va-drivers]
      debian-stable: [libgl1-mesa-dri, libglx-mesa0, libegl-mesa0, mesa-vulkan-drivers, mesa-va-drivers]
      fedora-latest: [mesa-dri-drivers, mesa-libGL, mesa-libEGL, mesa-vulkan-drivers, mesa-va-drivers]
      arch: [mesa]

  - id: vulkan-loader
    provides:
      ubuntu-lts: [libvulkan1]
      debian-stable: [libvulkan1]
      fedora-latest: [vulkan-loader]
      arch: [vulkan-icd-loader]

  - id: libva
    provides:
      ubuntu-lts: [libva2, libva-drm2, libva-x11-2]
      debian-stable: [libva2, libva-drm2, libva-x11-2]
      fedora-latest: [libva]
      arch: [libva]

  - id: intel-media-driver
    depends_on: [libva]
    arch: x86
    provides:
      ubuntu-lts: [intel-media-va-driver]
      debian-stable: [intel-media-va-driver]
      fedora-latest: [libva-intel-media-driver]
      arch: [intel-media-driver]

  - id: jemalloc
    provides:
      ubuntu-lts: [libjemalloc2]
      debian-stable: [libjemalloc2]
      fedora-latest: [jemalloc]
      arch: [jemalloc]

  - id: sdl3
    provides:
      ubuntu-lts: [libsdl3-0]
      debian-stable: [libsdl3-0]
      fedora-latest: [SDL3]
      arch: [sdl3]

  - id: zlib-ng
    provides:
      ubuntu-lts: [zlib1g]
      debian-stable: [zlib1g]
      fedora-latest: [zlib-ng-compat]
      arch: [zlib]

  - id: zstd
    provides:
      ubuntu-lts: [libzstd1]
      debian-stable: [libzstd1]
      fedora-latest: [zstd]
      arch: [zstd]

  - id: ffmpeg
    depends_on: [zlib-ng, zstd, libva, vulkan-loader, libdrm]
    provides:
      ubuntu-lts: [libavcodec60, libavformat60, libavutil58, libavfilter9, libswscale7, libswresample4]
      debian-stable: [libavcodec61, libavformat61, libavutil59, libavfilter10, libswscale8, libswresample5]
      fedora-latest: [ffmpeg-free]
      arch: [ffmpeg]
